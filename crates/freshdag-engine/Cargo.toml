[package]
name = "freshdag-engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Graph engine: validity evaluation, invalidation, equivalence-based early cutoff, recomputation scheduling."

[lints]
workspace = true

[dependencies]
freshdag-core.workspace = true
freshdag-store.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
time.workspace = true
tracing.workspace = true
uuid.workspace = true

[dev-dependencies]
# Dev-only: proves a real `Probe` implementation arbitrates through
# `ProbeRegistry` and drives `Engine::check`. The engine itself depends
# on no probe crate — invariant #14 / "do not depend on a specific
# adapter or observer" applies to probes too.
freshdag-probes.workspace = true
